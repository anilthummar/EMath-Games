/// Build environment.
enum Environment { stage, production }

/// HTTP method type.
enum ApiType { get, post, delete }

/// Generic async state status.
enum BaseStateStatus { initial, loading, success, failure }
