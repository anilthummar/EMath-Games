name: graphql_client
description: GraphQL layer - client, response handling, base repository.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gql: ^1.0.1
  graphql_flutter: ^5.3.0
  network:
    path: ../network

dev_dependencies:
  flutter_lints: ^2.0.3
