name: localization
description: Reusable localization - locale persistence, delegate helpers for multi-project use.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  storage:
    path: ../storage

dev_dependencies:
  flutter_lints: ^2.0.3
