name: notifications
description: Notification setup - Firebase, Awesome Notifications, push.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  awesome_notifications: ^0.10.1
  firebase_core: ^3.1.1
  firebase_messaging: ^15.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.3
