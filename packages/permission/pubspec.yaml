name: permission
description: Permission handling - request, check, device info.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  device_info_plus: ^9.1.2
  flutter:
    sdk: flutter
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_lints: ^2.0.3
